# RailSafe AI — asosiy kutubxonalar (GPU/CPU dan mustaqil)
# Python 3.10+ talab qilinadi
#
# Bu faylni to'g'ridan-to'g'ri o'rnatmang — quyidagilardan birini ishlating:
#   GPU (NVIDIA):  pip install -r requirements-gpu.txt
#   CPU (faqat):   pip install -r requirements-cpu.txt
# yoki oddiygina:  install.bat  (avtomatik GPU/CPU aniqlaydi)

# --- GUI ---
PyQt6==6.10.2
PyQt6-WebEngine==6.10.0

# --- Video / tasvir ---
opencv-python==4.9.0.80
av==16.1.0
pillow==12.1.1
numpy==1.26.4

# --- PLC (Siemens S7) ---
python-snap7==2.1.0

# --- Hisobotlar ---
python-docx==1.2.0

# --- Konfiguratsiya / yordamchi ---
PyYAML==6.0.3
psutil==7.2.2
python-dateutil==2.9.0.post0
